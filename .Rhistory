##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
##NOTE: step 2; merge the directory with the filenumber
#we will take our filenumber and combine it with the directory variable
#to retrieve the full path+filename for reading the file into R
filename <- paste(directory,"/", filenumber, ".csv", sep="")
##NOTE: step 3; retrieve the data from the csv file(s) and place the contents
#of each file into a frame, inside a list.
temp <- read.csv(filename)
tempvector <- temp[, pollutant]
storage <- append(storage, tempvector)
}
mean(storage, na.rm=TRUE)
source('C:/Users/Martijn/R programming Assignments/pollutantmean.R')
source("pollutantmean.R")
pollutantmean("specdata", "sulfate", 1:10)
source('C:/Users/Martijn/R programming Assignments/pollutantmean.R')
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean("specdata", "nitrate", 23)
source("http://d396qusza40orc.cloudfront.net/rprog%2Fscripts%2Fsubmitscript1.R")
source("submitscript1.R")
submit()
submit()
submit()
submit()
read.table(text="", col.names = c("col1", "col2"))
df <- read.table(text="", col.names = c("id", "nobs"))
View(df)
df
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
}
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
id <- c(1,12,102)
id <-c(1,12,102)
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
directory <- "specdata"
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
}
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
getwd()
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename) }
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
}
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
}
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate))
}
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na(sulfite)| is.na(nitrate)))
}
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na("sulfite")| is.na("nitrate")))
}
View(subsettemp)
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- subset(temp,
!(is.na("sulfite") && !is.na("nitrate")))
}
na.omit(subsettemp)
subsettemp <- na.omit(subsettemp)
View(subsettemp)
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- na.omit(temp)
}
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- na.omit(temp)
storage <- append(storage, subsettemp)
}
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- na.omit(temp)
df <- append(df, subsettemp)
}
df <- read.table(text="", col.names = c("id", "nobs"))
df[1,1] <-id[i]
length(subsettemp)
nrows
nrows(subsettemp)
nrow(subsettemp)
directory <- "specdata"
id <-c(30:25)
#init dataframe to specs
df <- read.table(text="", col.names = c("id", "nobs"))
#for loop
for(i in seq_along(id)) {
##NOTE: step 1; format the filename.
# we will do this with a nested elseif to cover the three possible scenarios:
#scenario 1: id is between 1 and 9 and we need to prefix two zeros
#scenario 2: id is between 10 and 99 and we need to prefix one zero
#scenario 3: id is greater than 99, and we do not need to prefix
#
#this is true because the file format is "xxx.csv"
if(id[i] <= 9) {
filenumber <- paste("00", id[i], sep="")
} else if(id[i] <= 99) {
filenumber <- paste("0", id[i], sep="")
} else {
filenumber <- paste(id[i], sep="")
}
filename <- paste(directory,"/", filenumber, ".csv", sep="")
temp <- read.csv(filename)
subsettemp <- na.omit(temp)
df[i,1] <-id[i]
df[i,2] <-nrow(subsettemp)
}
View(df)
source('C:/Users/Martijn/R programming Assignments/complete.R')
complete("specdata", 1)
source('C:/Users/Martijn/R programming Assignments/complete.R')
complete("specdata", 1)
source("http://d396qusza40orc.cloudfront.net/rprog%2Fscripts%2Fsubmitscript1.R")
submit()
submit()
submit()
directory <- specdata
directory <- "specdata"
filenames <- list.files("specdata", pattern="*.csv", full.names=TRUE)
for(i in seq_along(filenames)) { print(i)}
for(i in seq_along(filenames)) { print(filenames[i])}
for(i in seq_along(filenames)) {
tempframe <- read.csv(filenames[i])
}
View(tempframe)
head(tempframe)
na.omit(tempframe)
tempframe <- na.omit(tempframe)
for(i in seq_along(filenames)) {
tempframe <- read.csv(filenames[i])
}
for(i in seq_along(filenames)) {
tempframe <- na.omit(read.csv(filenames[i]))
}
for(i in seq_along(filenames)) {
tempframe <- na.omit(read.csv(filenames[i]))
if(nrow(tempframe) > threshold) {
print("bigger")
}
else {
print("smaller")
}
}
threshold <- 12
for(i in seq_along(filenames)) {
tempframe <- na.omit(read.csv(filenames[i]))
if(nrow(tempframe) > threshold) {
print("bigger")
}
else {
print("smaller")
}
}
?cor
cor(tempframe)
cor(tempframe[,2])
cor(tempframe[,2], tempframe[,3])
results <- numeric()
#first, retrieve all the filenames, in case the instructor is being sneaky,
#and has more files in his specdata folder than I do...
filenames <- list.files(directory, pattern="*.csv", full.names=TRUE)
#for loop
for(i in seq_along(filenames)) {
tempframe <- na.omit(read.csv(filenames[i]))
if(nrow(tempframe) > threshold) {
results <- append(results, cor(tempframe[,2], tempframe[,3]))
}
else {
next
}
}
source('C:/Users/Martijn/R programming Assignments/corr.R')
source('~/.active-rstudio-document')
cr <- corr("specdata", 150)
head(cr)
source('C:/Users/Martijn/R programming Assignments/corr.R')
cr <- corr("specdata", 150)
head(cr)
source('C:/Users/Martijn/R programming Assignments/corr.R')
cr <- corr("specdata", 150)
head(cr)
source('C:/Users/Martijn/R programming Assignments/corr.R')
cr <- corr("specdata", 150)
head(cr)
summary(cr)
cr <- corr("specdata")
summary(cr)
length(cr)
source("http://d396qusza40orc.cloudfront.net/rprog%2Fscripts%2Fsubmitscript1.R")
submit()
submit()
submit()
